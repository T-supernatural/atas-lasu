import { NavLink, Outlet } from "react-router-dom";
import { useAuth } from "../auth/AuthProvider";

const publicNavigation = [["/", "Home"], ["/about", "About"], ["/membership", "Membership"]];
const memberNavigation = [["/", "Home"], ["/about", "About"], ["/dashboard", "Dashboard"]];
const adminNavigation = [["/", "Home"], ["/about", "About"], ["/admin", "Admin"]];

export function AppShell() { const { user, role, loading } = useAuth(); const navigation = !user ? publicNavigation : role === "admin" ? adminNavigation : memberNavigation; const action = user ? [role === "admin" ? "/admin" : "/dashboard", role === "admin" ? "Admin dashboard" : "Dashboard"] : ["/membership", "Join ATAS"]; return <div className="min-h-screen bg-stage-obsidian text-chalk-cream"><header className="sticky top-0 z-50 border-b border-line-subtle bg-stage-obsidian/95 backdrop-blur text-chalk-cream"><div className="mx-auto flex min-h-20 max-w-7xl items-center justify-between gap-5 px-6 py-5"><NavLink to="/" className="font-display text-2xl leading-none">ATAS<span className="text-theatre-amber">—</span>LASU</NavLink><nav className="hidden gap-5 text-sm text-chalk-cream/80 md:flex">{navigation.map(([to, label]) => <NavLink key={to} to={to} className={({ isActive }) => isActive ? "text-theatre-amber" : "hover:text-chalk-cream"}>{label}</NavLink>)}</nav>{loading ? <span className="text-xs text-stone-muted">Loading</span> : <NavLink to={action[0]} className="stage-link">{action[1]} <span>→</span></NavLink>}</div></header><main><Outlet /></main><footer className="border-t border-line-subtle bg-stage-charcoal px-6 py-12 text-chalk-cream/80"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-6 sm:flex-row"><div><p className="font-display text-2xl text-chalk-cream">ATAS<span className="text-theatre-amber">—</span>LASU</p><p className="mt-3 text-sm">Building dreams on stage, in class, and beyond.</p></div><address className="not-italic text-sm leading-6">ataslasuofficial@gmail.com<br />LASU, Ojo Campus</address></div></footer></div>; }
