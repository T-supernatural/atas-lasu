export function Pagination({ page, pageCount, onChange }) { if (pageCount <= 1) return null; return <nav aria-label="Pagination" className="mt-12 flex items-center justify-between border-t border-line-subtle pt-5 text-sm"><button type="button" disabled={page === 1} onClick={() => onChange(page - 1)} className="stage-link disabled:pointer-events-none disabled:opacity-35">← Previous</button><span className="text-xs font-semibold tracking-[.12em] text-chalk-cream/70">PAGE {page} / {pageCount}</span><button type="button" disabled={page === pageCount} onClick={() => onChange(page + 1)} className="stage-link disabled:pointer-events-none disabled:opacity-35">Next →</button></nav>; }
