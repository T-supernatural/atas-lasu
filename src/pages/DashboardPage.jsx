import { Link } from "react-router-dom";
import { useAuth } from "../auth/AuthProvider";

const destinations = [["Resources", "Study material, scripts, and shared references.", "/resources"], ["Events", "What is happening next in the ATAS community.", "/events"], ["Community chat", "Join the conversation with fellow members.", "/chat"]];
export function DashboardPage() { const { profile, user } = useAuth(); const name = profile?.full_name || user?.user_metadata?.full_name || user?.email?.split("@")[0] || "Member"; return <section className="bg-stage-obsidian px-6 py-16 sm:px-8 sm:py-24"><div className="mx-auto max-w-7xl"><p className="text-xs font-semibold uppercase tracking-[.12em] text-theatre-amber">Member desk</p><h1 className="mt-5 font-display text-5xl leading-[.94] text-chalk-cream sm:text-6xl">Welcome back, <em className="font-normal text-theatre-amber">{name}.</em></h1><p className="mt-6 max-w-xl text-[15px] leading-7 text-chalk-cream/85">Your ATAS-LASU space for what is useful, what is next, and who is in the room.</p><div className="mt-14 grid gap-5 lg:grid-cols-3">{destinations.map(([title, text, to], index) => <Link key={title} to={to} className="stage-card group flex min-h-64 flex-col p-7"><p className="text-xs font-bold tracking-[.12em] text-theatre-amber">0{index + 1}</p><h2 className="mt-auto font-display text-4xl text-chalk-cream">{title}</h2><p className="mt-3 max-w-xs text-sm leading-6 text-chalk-cream/80">{text}</p><span className="stage-link mt-7">Open {title} <span>→</span></span></Link>)}</div></div></section>; }
