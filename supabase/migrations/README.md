# Supabase migrations

Place every database schema, function, storage-policy, and Row Level Security change here as an ordered SQL migration.

The existing production database is not modified in Phase 0. The Phase 5 migration will document and enforce the role-based policies required for the admin dashboard.
